from .model import Wire

class PackedReader:
    def __init__(self, path: str, max_record_bytes: int = ...) -> None: ...
    def header_json(self) -> str: ...
    def read_index(self, index: int) -> Wire: ...
    def read_uid(self, uid: str) -> Wire: ...
    def close(self) -> None: ...

class PackedWriter:
    def __init__(
        self,
        path: str,
        categories_json: str,
        metadata_json: str,
        samples: int,
        compression_level: int = ...,
        max_record_bytes: int = ...,
    ) -> None: ...
    def push(self, record: bytes) -> None: ...
    def finish(self) -> None: ...
    def close(self) -> None: ...

def mask_crops(
    record: bytes,
    classes: list[int] | None = ...,
    padding: int = ...,
    fill: tuple[int, int, int] = ...,
    object_index: int | None = ...,
    mask: bytes | None = ...,
) -> list[Wire]: ...
def transform(record: bytes, operation: str, parameters_json: str) -> Wire: ...
